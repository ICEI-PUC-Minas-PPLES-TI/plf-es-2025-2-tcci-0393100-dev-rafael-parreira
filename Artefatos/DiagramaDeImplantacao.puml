@startuml

node "Servidor Frontend\n(Vercel / Netlify)" as Frontend {
    artifact "React SPA\n(build estático)" as ReactApp
}

node "Servidor Backend\n(Render / AWS EC2)" as Backend {
    artifact "Node.js API\n(PM2 + Docker)" as NodeApp
    database "PostgreSQL\n(gerenciado)" as DB
}

cloud "Internet" {
    component "API Videoconferência\n(Jitsi/Zoom/WebRTC)" as VideoAPI
}

' --- CORREÇÃO AQUI ---
' A nota agora é definida diretamente à direita do Frontend
note right of Frontend : "Hospedagem estática\nBuild automático via GitHub"

note "Container Docker\nPM2 para gerenciamento" as Note2
Backend .. Note2


ReactApp --> NodeApp : "HTTPS (REST)"
NodeApp --> VideoAPI : "WebSocket / HTTPS"
NodeApp --> DB : "TCP 5432 (SQL)"

@enduml